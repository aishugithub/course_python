# course_python ("Foothold") — Hand-off Document

**Last updated:** 2026-07-04. Modules 1–10 complete (four pillars M5–M8, plus M9 files/exceptions, M10 OOP with Crucibles). A new **Computational Thinking interlude strand** was planned this day and its first unit built — see the CT interludes section near the end. The 2026-07-01 shell/product pass (rebrand, guest access, self-registration, analytics, mobile-friendly nav) is documented below and unchanged since.
**Purpose:** paste this whole file (or point Claude at it) at the start of a new conversation to resume work — whether that's building the next lesson module or continuing shell/infra work — without losing any context.

## What this project is

`course_python` is a React + Vite single-page app that delivers an interactive, self-paced Python course, publicly branded as **"Foothold"** (tagline: *"Have you got your foothold on Python yet?"*). It started as a closed course for B.Tech students at Sri Ramachandra Faculty of Engineering and Technology, and as of 2026-07-01 is being repositioned as a free course for **any novice programmer worldwide** — no account required to browse. It lives at `C:\aishu\courses\course_python`, is a git repo, and deploys to GitHub Pages at `https://aishugithub.github.io/course_python` via `npm run deploy` (which runs `vite build` then `gh-pages -d dist`). Login, self-registration, per-student progress, and usage analytics all go through a Google Apps Script backend (`Code.gs`, deployed separately from the frontend), configured in `config/gas.config.js` and called from `src/shell/api.js` / `src/shell/analytics.js`.

## Architecture — the things that matter

The whole app is built around one simple idea: lessons are fully independent, self-contained React components, and one config file decides which ones exist and in what order.

`config/course.config.js` is the only file you edit to change the course's structure. It exports `COURSE_CONFIG`, an object with `courseId`, `courseTitle`, `subtitle`, `batch`, and a `modules` array. Each module has a `moduleId`, `moduleTitle`, and a `units` array of `{ unitId, title }` pairs. `unitId` must exactly match a filename in `src/lessons/` (without the `.jsx` extension) — that's the only link between the config and the actual lesson code.

`src/lessons/UnitX_Y.jsx` — one file per lesson. Every lesson file is completely self-contained: its own local colour-palette constant, its own widgets, zero imports from other lesson files or from each other. This is deliberate — it means any single lesson can be rewritten, deleted, or handed to a different author without touching anything else. `src/shell/App.jsx` discovers every file in this folder automatically using Vite's `import.meta.glob('../lessons/*.jsx')`, so dropping a new `UnitX_Y.jsx` file in and adding a matching entry to `course.config.js` is the entire process for adding a lesson — no other file needs to change. **This session's shell changes (below) never touched a single lesson file — that boundary is intentional and should stay intact.**

`src/shell/App.jsx` is the app's router/state machine. It owns a `view` state (`'landing' | 'login' | 'dashboard' | 'lesson'`), the `student` object (`null` = browsing as a guest), `completedUnits`, and the currently active lesson. It dynamically imports whichever lesson file matches the unit clicked, and renders it with two props: `student` and `onUnitComplete` (a callback the lesson must call exactly once, when the learner finishes — this both persists progress and fires a `lesson_complete` analytics event).

`src/shell/Dashboard.jsx` renders every module and its units as clickable cards, reading straight from `COURSE_CONFIG`. Navigation is free — every unit is always clickable regardless of what's completed — `completedUnits` only controls the ✅/▶️ icon and the overall percentage bar. It's guest-aware: shows "Browsing as Guest" + a "sign in to save progress" prompt when `student` is `null`, or the student's name/roll number + a "Sign Off" button when signed in.

## The lesson template every unit follows

Every `UnitX_Y.jsx` file, without exception, has this shape: a header showing `MODULE X › UNIT X.Y` plus a live "N / total done" counter; a thin animated progress bar; a horizontal tab strip of 4-5 sections (the last one is always the quiz); a content card that renders whichever section is active; a "Mark Complete & Continue" button that advances to the next tab (hidden on the quiz tab, since the quiz has its own progression buttons); and a closing multiple-choice quiz with instant colour feedback (green for correct, red for the student's wrong pick) plus a one-line explanation after each answer, ending in a completion screen that shows a score, a contextual message, and previews the next unit or module. Only the quiz's `onComplete` callback calls `onUnitComplete` — that is the single point where a unit's completion is actually persisted.

Every file defines the exact same colour palette (dark, GitHub-inspired): background `#0D1117`, surface `#161B22`, card `#1C2333`, accent blue `#58A6FF`, solid-button blue `#1F6FEB`, success green `#3FB950`, warning yellow `#D29922`, purple `#BC8CFF` for "key insight" callouts, error red `#F85149`, plus orange/teal used for variety in some lessons. Copy this object verbatim into any new lesson file rather than inventing new colours.

## Teaching philosophy — how content gets designed, not just coded

Every widget teaches exactly one idea through interaction — clicking, dragging a slider, stepping through an animation, guessing before revealing — never through a wall of static text. Analogies come before jargon (a program is "a recipe," a variable is "a locker only you can open"). Each unit closes with a quiz that gives immediate feedback and an explanation, not just a score.

The one rule that most shaped Module 4, and should carry forward to every future unit that introduces a new core CS concept: **don't just show the syntax — motivate the need for it first, contrast it with a lower-level language (usually C) to show what Python is actually doing for you, and where relevant, visualise what's happening in memory or at the hardware level.** This came directly from feedback while building Unit 4.2 (Variables & Memory): before naming "variables," the lesson proves why one is needed at all (a football score-counter that literally cannot count without one), then shows an actual row of numbered memory slots being reserved and reused, and only then contrasts `score = 0` in Python against `int score = 0;` in C — framing dynamic typing as a genuine advantage rather than a syntax quirk. Apply this same need → lower-level contrast → memory/hardware visualisation sequence to future concepts: functions and the call stack, lists as contiguous memory, references/aliasing, recursion.

The whole course is explicitly built on **Vygotsky's Zone of Proximal Development and scaffolding theory** — this is now stated on the public Landing page (see below) and is genuinely how content should keep being designed: introduce a new concept only once the learner has exactly the prior pieces needed to understand it, provide just enough support (an analogy, a visualization, a guided interaction) to carry them across, then let that support fall away as the next concept builds on this one. If Aishu ever wants to write this course up as education research, this framing — and the "need → lower-level contrast → visualisation" sequence above — is the load-bearing pedagogical claim to build on.

Units are built and delivered one at a time, not batched — each finished `.jsx` file is copied into `src/lessons/` and shown to Aishu immediately, before starting the next unit. Keep doing this for future modules.

**Comment style:** Module 4's files (`Unit4_1.jsx`–`Unit4_4.jsx`) carry heavy, line-by-line explanatory comments — that was requested at the time to document the app's architecture while it was being established. Starting with Module 5, Aishu asked to stop that: the conventions are now well understood and no longer need re-explaining in every file. From `Unit5_1.jsx` onward, lesson files use only light section-divider comments (e.g. `// ── Widget Name ──`), matching the original pre-Module-4 style (see `Unit1_1.jsx`, `Unit2_4.jsx`, `Unit3_3.jsx`). Do not retroactively strip Module 4's comments; just don't add that density of comments to new files. **This same light-comment convention was used for all the shell files touched this session** (`App.jsx`, `Login.jsx`, `Dashboard.jsx`, `Landing.jsx`, `analytics.js`, `guestProgress.js`, `Code.gs`) — a short explanatory block only where something genuinely non-obvious is happening (e.g. why `handleRegister` reuses the Students sheet's columns positionally), not line-by-line.

## Confirmed roadmap: Modules 5-8

Locked in with Aishu on 2026-07-01, following the classic "four pillars" of a first programming course (same skeleton as CS50 and most standard Python textbooks): **Module 5 = Selection** (done), **Module 6 = Repetition** (done 2026-07-03), **Module 7 = Organizing Data** (done 2026-07-03), **Module 8 = Functions & Modular Thinking** (done 2026-07-03, confirmed by Aishu before building, see below). ALL FOUR PILLARS ARE NOW COMPLETE. The natural continuation (not yet planned in detail, propose and confirm with Aishu first): a Module 9 on exceptions + file I/O — Unit 8.4's full-code section explicitly plants both seeds (int("eighty") crashes the app; data is forgotten on exit) and its completion screen promises "making programs tough and permanent". After that: basic OOP, then domain branches into Aishu's AI/ML, cybersecurity, and IoT interests.

## Course status as of this hand-off

Modules 1-7 are done. Module 1 (Computing), Module 2 (Computing Languages), Module 3 (The Conversion: compilation/interpretation/PVM) — as before. Module 4 (Your First Python Program, 4 units: print/comments, Variables & Memory, Input & Type Conversion, Operators & Expressions + Simple Interest capstone). Module 5 (Making Decisions, 4 units): Booleans & Truthy/Falsy, if/elif/else, Nested Conditions & Boolean Logic, Capstone: Decision Maker. Module 6 (Repetition, 4 units, done 2026-07-03): The while Loop, The for Loop & range(), break/continue & Nested Loops, Capstone: Number-Guessing Game. **Module 7 (Organizing Data, 5 units) — done 2026-07-03:** 7.1 Strings in Depth (indexing on clickable character slots, slicing playground on roll number "22CS1042" with explicit range() callback, immutability gotcha with live TypeError + build-new-string fix), 7.2 Lists (60-students need slider, list-as-slots anatomy with C array contrast, in-place mutation buttons contrasted against 7.1's TypeError, class-average build with `for m in marks`), 7.3 List Methods & Loop Patterns (live method toolbox append/remove/pop/sort, two step-through traces — count-the-passes and find-the-topper/best-so-far — and a sort+slice merit-list build), 7.4 Tuples & Dictionaries (tuple-as-locked-list with silent-corruption vs loud-TypeError toggle, parallel-lists-vs-dict need, lockers-with-names dict widget incl. KeyError gotcha and `in` checks keys, dict+loop+if grade-report build), 7.5 Capstone: Student Marks Manager (mission checklist mapping all 7 tools to their units, v1→v2→v3 build-in-steps, playable manager widget with live dict view and console, full runnable listing + 3 challenge upgrades). Unit 7.5's completion screen announces "Module 7 Complete!" and previews Module 8 — Functions. **Module 8 (Functions & Modular Thinking, 4 units) — done 2026-07-03:** 8.1 Why Functions? (copy-paste trap slider on 7.5's report block, clickable def anatomy with C contrast, flagship trace showing def SKIPS the body and calls jump in/return — "definition stores, calling runs", PA-system build with function+loop), 8.2 Parameters & Return Values (one-function-per-message need, arguments-fill-parameter-lockers visual with order-matters warning, return-vs-print vending-machine toggle with the None trap, grade() machine over the M7 dict with slider), 8.3 Scope & the Call Stack (predict-first x=10/x=99 mystery widget — prints 10!, two-rooms local/global visual with during/after toggle and one-way-glass note for reading globals, flagship call-stack trace with frames stacking/popping through bill_line→add_gst, vanished-local NameError gotcha fixed by return), 8.4 Grand Capstone: Marks Manager 2.0 (mission checklist spanning 4 modules, REFACTOR-in-steps v1 monolith→v2 extract functions→v3 compute-vs-display split with class_stats returning a tuple indexed by position, playable manager with live call-log panel, full runnable listing + tuple-unpacking bonus peek + 3 challenges incl. refactoring the 6.4 guessing game). Unit 8.4's completion screen announces "Module 8 Complete — ALL FOUR PILLARS!" and previews exceptions + files. All Unit7_X and Unit8_X files follow the standard template (identical palette, 5 tabs, 4-question quiz, light comments) and were syntax-checked with standalone esbuild; Aishu still needs to run `npm run build` / `npm run deploy` locally. No shell files or Code.gs were touched for Modules 6-8 — lesson files + course.config.js only, so no Apps Script redeploy is needed.

## Environment-setup unit — Unit 4.0 (BUILT 2026-07-04)

Aishu flagged that the course never teaches students to run Python themselves (everything is browser widgets + Pyodide). Decision: a **Unit 4.0 "Running Python on Your Own Machine"**, inserted as the FIRST unit of Module 4, *before* Unit 4.1 — purely additive, renumbers nothing (4.1–4.4 unchanged). Environments to teach chosen by Aishu = **IDLE + VS Code**. Framing rule she was explicit about: students have NOT been writing code themselves — the site only *shows* code — so the copy must say "you're free to learn entirely in this browser; but if you want, you can also run Python on your own machine — here's how (IDLE or VS Code)." Optional/open-door, never a hard gate, to preserve the browser-first no-install promise. `src/lessons/Unit4_0.jsx`, esbuild-clean, registered in `M4` before Unit4_1. 5 sections: **Two Ways** (browser vs own machine, click-to-compare, both valid; callback to Module 3's interpreter), **Install & REPL** (3 install steps incl. "Add Python to PATH"; interactive **mock REPL** the student types into — handles `print("...")` and one simple arithmetic op incl. //, **, %, float `/`, ZeroDivisionError; preset chips), **REPL vs Script** (toggle: throwaway interactive vs saved hello.py that persists — seeds Module 9 files), **IDLE & VS Code** (toggle with numbered run-a-.py steps for each; "same Python, different desk"). Quiz teases Unit 4.1. Light comments (current convention) even though Module 4's original files use heavy comments. Note: mock REPL is a JS simulation, not real Python — clearly labelled as such.

## Shell/product work completed 2026-07-01 (separate from lesson content)

This was a distinct pass of work, prompted by Aishu wanting to open the course to the public. None of it touched `src/lessons/*.jsx`.

**Rebrand:** "SRET E-Learning" / "Sri Ramachandra Faculty of Engineering and Technology" branding removed from `Login.jsx` and `Dashboard.jsx`, replaced with **"Foothold"** and the tagline "Have you got your foothold on Python yet?"

**Guest-first access (new `src/shell/Landing.jsx`, rewired `App.jsx`):** The app now opens on a Landing page, not a login wall. Landing explains what the course is, who it's for, states the ZPD/scaffolding pedagogy in plain language, and offers two paths: "Explore the Dashboard" (goes straight in as a guest, `student = null`) or "Sign In / Register". Guests can browse and complete every lesson with zero friction; their progress is tracked client-side only, via `src/shell/guestProgress.js` (a small localStorage helper, key `foothold_guest_progress_v1`), not in Sheets. Signing in is purely optional and only buys you progress-sync across devices.

**Self-registration (`Code.gs`: new `register` action / `handleRegister`; `Login.jsx`: Sign In / Create Account toggle; `api.js`: `registerStudent()`):** Anyone can now create an account without Aishu manually adding a row to the Students sheet. The email address doubles as the username; the password is a new one invented just for this site (the registration form carries an explicit, visible warning that it is **not** the person's email account password — this was a specific, deliberate requirement from Aishu, worth preserving in any future redesign of that screen). Under the hood, `handleRegister` reuses the exact same five Students-sheet columns `handleLogin` already reads positionally (`[rollNo/username, password, name, batch, email]`) — a self-registered user's row just has their email in the "rollNo" slot and `"Self-registered"` in the "batch" slot. No sheet schema change was needed or made.

**Sign Off:** `Dashboard.jsx` now has a "Sign Off" button (only shown when signed in, not for guests) that clears `student`/`completedUnits` in `App.jsx` and returns to the Landing view.

**Mobile-friendly lesson navigation:** The original "← Dashboard" fix used a text pill button plus a 150px left-padding shift of the whole lesson — Aishu flagged this as wasting too much width on phone screens. **Current, correct version:** a small 36px circular icon-only button (just "←") fixed at the top-left corner, with `title`/`aria-label="Back to Dashboard"` for a hover tooltip on desktop, and zero layout shift — lessons render full-bleed on every screen size, exactly as before this whole nav investigation started. If this ever gets revisited, don't reintroduce a permanent margin/shift; the icon-only + tooltip approach was the agreed-on fix.

**Usage analytics (new `src/shell/analytics.js`; `Code.gs`: new `logEvent` action / `handleLogEvent`):** A minimal, deliberately small event set — `session_start` (once per app load, fired from a `useEffect` in `App.jsx`), `lesson_open` (in `handleSelectUnit`), `lesson_complete` (in `handleUnitComplete`), and `signup` (in `Login.jsx`'s `handleRegister`, only on a genuine new account, not every sign-in). Every event carries an anonymous ID (`foothold_anon_id` in localStorage, generated once via `crypto.randomUUID()`) that persists across guest browsing *and* after a later sign-up — this is what makes a real guest→signup conversion trace possible, not just a raw signup count. `handleLogEvent` auto-creates an `Events` tab in the spreadsheet on first call (columns: `Timestamp, AnonId, UserId, EventType, CourseId, UnitId`) — no manual sheet setup required. **Design rule that must be preserved in any future analytics work: `logEvent()` is fire-and-forget and never awaited by its callers — a slow or failed analytics call must never block or break the actual learning experience.** Verified live 2026-07-01: `session_start` and `lesson_open` are confirmed working end-to-end (real rows seen in the Events tab). `lesson_complete` has not yet been confirmed live — Aishu should finish one lesson's quiz and check the Events tab to close that loop. A Looker Studio dashboard on top of the Events tab was agreed on as the reporting layer but has **not been built yet** — the tab is just raw rows today.

**Important operational note — Code.gs must be manually redeployed:** Every `Code.gs` change this session (register, logEvent) requires Aishu to paste the updated code into the Apps Script editor (Extensions → Apps Script from the Sheet) and do Deploy → Manage deployments → edit → **New version** → Deploy. Editing the file alone, or anything on the frontend, does **not** push Apps Script changes live. If a new backend feature "isn't working," check this first before debugging code.

**Sandbox limitation, for whoever picks this up next:** this project's `node_modules` were installed on Windows; a Linux sandbox (if used again) cannot run `vite build`/`esbuild` against them (`Cannot find module @rollup/rollup-linux-x64-gnu`, `@esbuild/win32-x64` platform mismatch). All verification of shell changes this session was done by manually re-reading files, not by running a build. Aishu runs `npm run build` / `npm run dev` locally to actually confirm things compile.

## The Crucible — gated challenge units (started 2026-07-03, M4 pilot built)

**"The Crucible — where knowledge is forged into skill"** is a gamified, OPTIONAL challenge unit at the end of each module (after the capstone), starting from Module 4. Aishu's requirements, all implemented: gated stages unlocked one by one, hints instead of answers, per-stage analytics, and — critically — **lesson completion is never affected: the Crucible is bonus content and excluded from the dashboard percentage.**

Design (see `Unit4_C.jsx`, the pilot): four stages, metallurgy arc — **Spark** (predict-the-output MCQs) → **Flame** (bug hunt: click the guilty line, then pick the fix) → **Forge** (Parsons problem with ↑↓ reorder, validated by variable DEPENDENCIES so any legal ordering passes) → **Temper** (write real Python in a textarea, executed in-browser via **Pyodide** lazy-loaded from the jsdelivr CDN only when the stage is reached; two test cases incl. one with hidden values to defeat hard-coding; graceful fallback to a mini-Parsons if Pyodide can't load). Wrong answers reveal staged hints (never answers). Passing Temper shows a badge screen; a "Claim the badge" button calls onUnitComplete (NOT auto-fired — onUnitComplete unloads the lesson back to the Dashboard, so the badge must be seen first).

Infrastructure — deliberately zero backend change, **no Code.gs redeploy needed**: stage completions persist as pseudo-unitIds like `Unit4_C@spark` through the EXISTING saveProgress/getProgress (Progress sheet) for signed-in students and guestProgress localStorage for guests. Analytics: each stage fires a `challenge_stage_complete` event with the pseudo-id in UnitId via the existing logEvent — the Events tab needs no schema change, and the stage funnel (who reached spark/flame/forge/temper) can be read straight off those rows. Shell changes made (the one deliberate exception to the don't-touch-shell rule, kept minimal): `App.jsx` gained handleStageComplete + passes `challengeProgress` and `onStageComplete` props to every lesson (regular lessons ignore them); `Dashboard.jsx` computes the course % from non-optional units only (this also filters out the @-pseudo-ids) and renders `optional: true` config units as orange 🔥 "BONUS · CHALLENGE" cards. Config: `{ unitId: "Unit4_C", title: "The Crucible", optional: true }`.

**All four remaining crucibles built and registered 2026-07-03: Unit5_C, Unit6_C, Unit7_C, Unit8_C** — the full M4-M8 foundation now has a Crucible after every capstone. Each follows the Unit4_C pilot exactly (same stage shell, gating, hints, pseudo-unitId persistence, badge-claim pattern), with per-module content respecting the scaffolding ledger, plus the two new question types: **trace-the-state** (typed answer, no options — from M5 on) and **spot-the-lie** (click the false claim among three — from M6 on). Both live inside the Spark stage as new `kind` values (`mcq` / `trace` / `lie`), not as separate stages. Notable per-module choices: M5 Forge/Temper use block-structure pseudo-tokens (`if_open`, `if_body`, `else_open`) in the defines/needs dependency checker so misplaced indented lines get structural error messages, not just locker messages — M6-M8 reuse this for loops and def blocks. M6 Flame is the infinite loop (counter stepping away from exit) + the off-by-one (`range(1, 10)` missing 10). M7 Temper's hidden test is an all-negative list, defeating the `biggest = 0` initialization. M8 Temper flips the harness: the student DEFINES `average(a, b, c)` and each test appends a `t.post` snippet that *calls* it (one test does math with the return value, so printing-instead-of-returning fails) — the only structural deviation from the pilot's `t.pre`-only harness. All five lesson files + config pass an esbuild syntax check; a real `npm run build` still needs to be run by Aishu locally (Windows node_modules, see sandbox limitation above).

## How to continue in a new conversation

Two kinds of work could come next — lesson content (Module 6+) or more shell/product work (Looker Studio, verifying `lesson_complete`, further UX) — read this whole document regardless of which one Aishu asks for.

**For lesson content:** read `config/course.config.js` to confirm current state, read one or two existing lesson files (e.g. `Unit8_3.jsx` for a concept unit with traces and `Unit8_4.jsx` for a capstone) to internalise the template, colour palette, and current (light) comment style, then propose a short plan for the next module's units — topic breakdown and widget ideas — before writing any code, the same plan-first approach used for Modules 4-8. Build and deliver one unit at a time, updating `course.config.js` as each is added. Keep every lesson file fully self-contained with no cross-lesson imports, and keep comments light. The natural next module, to propose and confirm with Aishu before building: **Module 9 — exceptions + file I/O** (suggested arc: 9.1 When Programs Crash / try-except, 9.2 Handling Bad Input Gracefully, 9.3 Files: Programs That Remember, 9.4 Capstone — e.g. the Marks Manager saving/loading its dict to a file and surviving any input; Unit 8.4 planted exactly these two seeds). Scaffolding ledger after Module 8: students know all of M4-M7 (variables/memory slots, input/int conversion, operators, if/elif/else + boolean logic, while/for/range/break/continue/nested, counter+accumulate patterns, string indexing/slicing/immutability, lists + methods + count/best-so-far/sort patterns, tuples incl. returning-a-tuple-and-indexing-it, dicts fully) plus def, calls, parameters vs arguments, return (incl. early return and the print-returns-None trap), local vs global scope, reading globals, and the call stack with frames. Tuple UNPACKING was shown only as a bonus peek in 8.4, not taught. Still unknown: f-strings, exceptions, files, classes, import (beyond the 6.4 random sneak peek).

**For shell/product work:** read `src/shell/App.jsx`, `Landing.jsx`, `Login.jsx`, `Dashboard.jsx`, `analytics.js`, `guestProgress.js`, `api.js`, and `Code.gs` directly (don't scan the whole repo — see the note above about reading only what's relevant). Known open items as of this hand-off: (1) confirm `lesson_complete` events actually reach the Events tab, (2) build the Looker Studio dashboard on top of the Events tab, (3) nothing else is currently pending — guest access, self-registration, sign-off, and the mobile nav fix are all done and confirmed working live.

## Computational Thinking interludes — the CT sets (planned 2026-07-04)

Aishu wants the course to teach **computational thinking**, not just syntax — students must learn to go problem → logic → code. Decided design: a **series of 3 (maybe 4) CT interlude modules**, each dropped in right after its prerequisites are met, so every classic "university lab-manual" program is solvable with what's already been taught. Labelled with `.5` module ids so **no already-built module (M7–M10) is renumbered**. The recurring visualization for the whole CT strand = the **accumulator panel** (a single box updated each loop round). Framework taught explicitly = the four cornerstones of computational thinking (Wing, CACM 2006): decomposition, pattern recognition, abstraction, algorithm design.

The three CT sets and their program coverage:
- **CT Set 1 — "Thinking in Algorithms" (module id `M6.5`, after M6 Repetition).** Number/loop programs (only loops + ints + operators, no lists/functions yet): sum 1..n, factorial, multiplication table, power without `**`, sum & count of digits, reverse a number (accumulator unit); prime check, palindrome number, Armstrong, perfect, strong, GCD/LCM (flag / search-in-loop unit); star/number patterns, Floyd's, Pascal's, primes-in-range (nested-loop unit); + capstone "walk the four moves yourself".
- **CT Set 2 — "Algorithms on Data" (module id `M7.5`, after M7 Organizing Data).** List/string/matrix: largest/smallest, second largest, sum/avg, remove duplicates, count occurrences; linear & binary search; bubble/selection/insertion sort (with step-through viz); matrix add/multiply/transpose; string palindrome, vowel/consonant count, word count, char frequency, reverse.
- **CT Set 3 — "Thinking Recursively" (module id `M8.5`, after M8 Functions).** Recursion (reuses M8 call-stack viz): recursive factorial, sum of digits, power; recursive Fibonacci + recursion-vs-iteration trade-off; Tower of Hanoi (showcase).
- Optional **CT Set 4** later (post-OOP or in M12) — hold until CT 1–3 land.

**Each CT set ends with its own Crucible** (Aishu confirmed 2026-07-04) — same gamified optional challenge format as the M4–M10 Crucibles (Spark → Flame → Forge → Temper, `optional: true`, pseudo-unitId persistence, no backend change). So CT Set 1 finishes with `UnitCT1_C`, drilling the number/loop programs (predict a sum/factorial, hunt an accumulator bug like init-inside-the-loop, Parsons-assemble an accumulator, Temper: write a sum/factorial). Build it after the CT1 content units are done.

CT lesson filenames use the pattern `UnitCT<set>_<unit>.jsx` (e.g. `UnitCT1_1.jsx`). Build gradually, ONE unit at a time, get Aishu's confirmation after each unit before moving to the next.

**CT Set 1, Unit 1 — BUILT & registered 2026-07-04.** `src/lessons/UnitCT1_1.jsx`, esbuild-clean, added to `course.config.js` as module `M6.5` (title "Computational Thinking I — Thinking in Algorithms"), first unit "The Four Moves & the Accumulator". 5 sections: **The Gap** (Need widget — `for i: print(i)` lists but doesn't solve; `total` box solves it), **The Four Moves** (click-to-reveal of the 4 cornerstones applied to sum 1..n, cites Wing 2006), **The Accumulator** (init→update→use anatomy + step-through trace of sum 1..5 with a live ACCUMULATOR/total panel — the module's recurring viz; key gotcha = init outside vs inside the loop), **Pattern Family** (toggle Sum / Factorial / Count-of-÷3 over one slider — same skeleton, different init+update; factorial starts at 1 not 0), Quiz (4 Qs). No f-strings. Standard template/palette, 🧠 header icon. **CT Set 1, Unit 2 — BUILT & registered 2026-07-04.** `src/lessons/UnitCT1_2.jsx`, esbuild-clean, added to `M6.5` as "Counting & Building with Loops". Teaches the **digit-peel pattern** (`n % 10` = last digit, `n // 10` = drop it, driven by `while n > 0`) — reusing % and // from Unit 4.4 and while from Unit 6.1. 5 sections: **Looping Digits** (Need — range() can't loop a number's digits, while+peel can), **The Digit Peel** (interactive ✂️ peeler on 4729 showing n%10 / n//10 and digits pulled off last-first), **Trace It** (step-through sum-of-digits of 4729 with live shrinking-n + accumulator panels), **Build It** (two-mode toggle: Reverse a Number via `rev = rev*10 + digit` on a while loop, and Power-without-`**` via a for loop — contrasts while-vs-for per Unit 6 rule), Quiz. No f-strings. **CT Set 1, Unit 3 — BUILT & registered 2026-07-04.** `src/lessons/UnitCT1_3.jsx`, esbuild-clean, added to `M6.5` as "Deciding Inside a Loop". Teaches the **flag / verdict pattern** (assume True → loop hunts for counter-evidence → decide once; `break` as optimisation once proof found). 5 sections: **The Need** (deciding-too-early prints many verdicts vs one flag = one verdict), **The Flag** (anatomy: init True / flip False on evidence / break / read after; prime-7 vs not-prime-15 mini contrast), **Trace It** (step-through "is 15 prime?" with live green/red FLAG panel, divisor i, 15%i), **Verdict Toolkit** (toggle Prime [flag] / Palindrome [reverse==n, reuses Unit 2] / Armstrong [digit peel + power, 153=1³+5³+3³] over one slider 1–999), Quiz. No f-strings. Scaffolding fix applied: Armstrong code counts digits with a peel loop (p), NOT len(str(n)) — len/str aren't taught until M7, which comes after this CT set. Config `M6.5` now holds UnitCT1_1–3.

**CT Set 1, Unit 4 — BUILT & registered 2026-07-04.** `src/lessons/UnitCT1_4.jsx`, esbuild-clean, added to `M6.5` as "Patterns & Nested Loops". Teaches **nested loops for patterns** (outer=rows, inner=row content; clock/hour-minute-hand analogy from Unit 6.3). Introduces one small new tool: `print(char, end="")` (default print adds a newline; end="" keeps output on the same line, bare print() ends the row) — needed because string repetition (`"*"*i`) and string concat aren't taught until M7, so end="" is the scaffolding-safe way to draw a row. 5 sections: **The Need** (one loop = a column, nested = a triangle, slider), **Nested Loops** (anatomy + end="" default-vs-override contrast), **Star Stepper** (step-through building a 4-row ★ triangle row by row with live OUTER i / INNER star# panels — the Grid/Nested-Stepper pattern), **Pattern Toolkit** (toggle ★ Triangle / Number Triangle / Floyd's [accumulator num counting across rows] / Primes [nested loop running Unit 3's prime flag] over a rows slider), Quiz. No f-strings. Pascal's triangle deliberately deferred (needs lists/combinations) — mentioned only as a "variation" in the wrap-up.

Config `M6.5` now holds UnitCT1_1–4. **Remaining CT Set 1:** Unit 5 capstone ("Crack It Yourself" — walk the four moves on a fresh problem; Unit 4's quiz teaser already promises this), then the CT Crucible `UnitCT1_C`. Aishu still runs `npm run dev`/`build`/`deploy` locally. **Note on config verification:** the Linux sandbox's read of `course.config.js` came back stale/truncated after the host Edit (known mount-staleness gotcha) — the edit was verified correct via the host Read tool, not the sandbox.

## SESSION UPDATE 2026-07-04 (b) — Lab-alignment build: CT strand + gap modules

**Context:** Aishu asked to build all pending lessons while aligning the course to her **Python Lab Manual** (MED23CL202, 15 experiments). A Course⇄Lab coverage audit found 7 gaps the course didn't yet teach: sets, modules/imports, abstraction, polymorphism, user-defined exceptions, pandas, and a whole data-visualization strand (Matplotlib/Seaborn/Plotly). Agreed plan (Aishu, away, said "do the whole plan, don't wait"): **CT interludes first**, then close the OOP/exception gaps, then sets+M11, then M12 Pandas, then a NEW M13 Data Visualization module — pandas + viz go into the **trunk** (not track-only) so every lab experiment is backed by a course lesson. Scope = course lessons only (lab notebooks are a separate effort).

**DONE this session (all esbuild-clean via standalone `npx esbuild@0.21.5 --loader:.jsx=jsx`, no f-strings, registered in course.config.js):**

- **Module 6.5 (Computational Thinking I) — NOW COMPLETE.** Added `UnitCT1_5.jsx` (Capstone: Crack It Yourself — walks the four moves on a fresh problem = count & sum primes ≤ N, unifying accumulator U1 + range loop + prime flag U3 + nesting U4; sections The Challenge / Decompose / Design It [tap-to-highlight code regions] / See It Run [step-through with n, is_prime, count, total panels] / Quiz) and `UnitCT1_C.jsx` (🔥 Crucible: Spark predict-sum/trace-digit-peel/spot-the-product-starts-at-1 lie · Flame reset-inside-loop + factorial-seeds-0 bugs · Forge assemble sum-of-digits while-loop · Temper write factorial accumulator, hidden n=0→1 test defeats fact=0, mini-Parsons fallback; badge teases M7). Config M6.5 now holds UnitCT1_1–5 + UnitCT1_C.

- **Module 7.5 (Computational Thinking II — Algorithms on Data) — NEW MODULE, COMPLETE.** Inserted in config between M7 and M8. Six files:
  - `UnitCT2_1.jsx` Scanning a List (single-pass reductions: max/min/sum/avg/count; seed-with-marks[0]-not-0 gotcha; Scan Stepper best-so-far).
  - `UnitCT2_2.jsx` Searching: Linear & Binary (found=-1 sentinel; binary Stepper with low/mid/high panels + shrinking range; linear-vs-binary comparison slider showing N vs log₂N; binary needs sorted).
  - `UnitCT2_3.jsx` Sorting: Bubble/Selection/Insertion (temp-swap taught explicitly since tuple-unpack not taught pre-M8; Bubble Stepper with growing green sorted zone; three-sorts toggle; notes real code uses .sort()).
  - `UnitCT2_4.jsx` Matrices: Grids of Data (list-of-lists, M[i][j], nested loops; Transpose Stepper (i,j)→(j,i); Matrix Toolkit add/transpose/multiply built with append — NO comprehensions since those are M11).
  - `UnitCT2_5.jsx` String Algorithms (string = read-only list of chars; reverse via rev=ch+rev accumulator; two-pointer Palindrome Stepper on "MADAM"; Text Toolkit reverse/vowel-count/word-count[split()]/char-frequency[dict tally]).
  - `UnitCT2_C.jsx` 🔥 Crucible (Spark best-so-far/vowel-count/binary-needs-sorted-lie · Flame seed-max-with-0 + swap-without-temp bugs · Forge assemble linear search · Temper write count-≥50 conditional accumulator, hidden all-fail→0 test, mini-Parsons fallback; badge teases M8).

**Scaffolding note for CT2:** M7.5 sits after M7 (lists/strings/dicts/split) but BEFORE M8 (functions) — so CT2 uses lists, indexing, len(), for-each, dicts, split(), while, and the temp-variable swap, but deliberately AVOIDS functions, tuple-unpacking swaps, and list comprehensions (all taught later). All result-matrices are built with append().

**STILL PENDING (the rest of the agreed plan — queued, NOT built):**
1. **CT Set 3 (module M8.5, after M8) — Thinking Recursively.** Recursion reusing M8's call-stack viz: recursive factorial / sum-of-digits / power; recursive Fibonacci + recursion-vs-iteration; Tower of Hanoi (showcase); + `UnitCT3_C` Crucible. Filenames `UnitCT3_1.jsx`… Build one at a time.
2. **Lab gap — Polymorphism & Abstraction** (extend M10; new unit(s) e.g. `Unit10_6.jsx`): method overriding as polymorphism, duck typing, abstract methods/NotImplementedError. Backs lab Exp 5 & 6.
3. **Lab gap — User-Defined Exceptions** (bridge M9+M10; e.g. a unit in M10 or M9): `class MyError(Exception)`, raise/except custom. Backs lab Exp 12.
4. **Lab gap — Sets** (short unit, e.g. append to M7 or M11) + **M11 Pythonic Python & Ecosystem** (import/modules, pip/venv, list comprehensions, generators, decorators-lite). Backs lab Exp 3.
5. **M12 Working with Real Data** (trunk bridge): datetime, JSON, NumPy intro, **Pandas**. Backs lab Exp 13. Ends by handing student a clean dataset — on-ramp to AI/ML track.
6. **M13 Data Visualization** (NEW trunk module): Matplotlib → Seaborn → Plotly. Backs lab Exp 14 & 15. This module was NOT in the prior roadmap; the lab manual forces it.

**Lab⇄Course coverage matrix (for reference):** Exp1 (types/ops/conditionals)=M4+M5 ✓; Exp2 (loops/strings)=M6+M7.1 ✓; Exp3 (collections+functions/modules)=M7+M8 ✓ but **sets & modules pending (#4)**; Exp4 (classes)=M10 ✓; Exp5 (encapsulation+abstraction)=M10.3 ✓ + **abstraction pending (#2)**; Exp6 (inheritance+polymorphism)=M10.4 ✓ + **polymorphism pending (#2)**; Exp7–9 (files/CSV)=M9.3–9.4 ✓; Exp10–11 (exceptions)=M9.1–9.2 ✓; Exp12 (user-defined exceptions) **pending (#3)**; Exp13 (Pandas) **pending (#5)**; Exp14–15 (Matplotlib/Seaborn/Plotly) **pending (#6)**.

Aishu still runs `npm run dev`/`build`/`deploy` locally (Windows node_modules — Linux sandbox can't run the project build; syntax verified with standalone esbuild only).

## SESSION UPDATE 2026-07-04 (c) — Full lab-gap build: CT3 + all lab-backing modules

Continued the 2026-07-04(b) plan to completion of the lab-alignment goal. **Every one of the 15 lab experiments now has a backing course lesson.** 9 more lesson files built this run (17 total across (b)+(c)), all registered in course.config.js.

**Module 8.5 (Computational Thinking III — Thinking Recursively) — COMPLETE.** UnitCT3_1 The Recursive Idea (base/recursive case, factorial call-stack stepper winding→unwinding reusing M8.3 viz, recursion-vs-loop), UnitCT3_2 Recursion in Action (sum-of-digits/power/countdown one skeleton, Fibonacci call-TREE with call-count explosion, iterative fib uses temp-swap not tuple-unpack), UnitCT3_3 Tower of Hanoi (peg-by-peg move stepper, 3 lines→7 moves, 2ⁿ−1 cost), UnitCT3_C Crucible (predict power / trace digit-sum / spot call-stack lie · missing-base-case + never-shrinks bugs · assemble recursive sum · Temper: DEFINE recursive sum_to(n), tests call it + do math; badge teases M9). **The entire CT strand (Sets 1-3) is now done.**

**Module 10 extended (backs lab Exp 5, 6, 12).** Unit10_6 Polymorphism & Abstraction (dispatch stepper: one s.area() call → different class methods; duck typing; abstract base raising NotImplementedError) — placed in config between Unit10_4 and Unit10_5. Unit10_7 User-Defined Exceptions (class MyError(Exception); raise/except by type; interactive mark-validator slider; exception hierarchy with a base MarksError) — placed after Unit10_6. Both breadcrumb "UNIT 10.6/10.7" though displayed as 5th/6th — unitId is just a filename key, display order = config order.

**Module 11 (Pythonic Python & the Ecosystem) — NEW.** Unit11_1 Sets (unique collections, {} vs set() trap, union/intersection/difference toggle, list-vs-set), Unit11_2 Modules & the Ecosystem (import / from-import / import-as toggle, your-own-.py-modules, pip + venv + PyPI). Backs lab Exp 3's sets & modules. (Comprehensions/generators NOT yet built — optional Pythonic extension, not lab-blocking.)

**Module 12 (Working with Real Data) — NEW.** Unit12_1 Meet Pandas: DataFrames (rendered DataFrame table widget; Series vs DataFrame; head/column-select/boolean-filter/describe toggle; pd.read_csv vs the M9 hand-split way). Backs lab Exp 13. (datetime/JSON/NumPy units NOT yet built — optional.)

**Module 13 (Data Visualization) — NEW.** Unit13_1 Charts with Matplotlib (the 5-line plt recipe; Bar/Line/Scatter/Pie toggle each with a LIVE inline SVG mini-chart + code; DataFrame.plot pipeline; teases Seaborn/Plotly). Backs lab Exp 14. (Seaborn + Plotly units NOT yet built — needed to fully back lab Exp 15.)

**Verification:** all files syntax-checked with standalone `npx esbuild@0.21.5 --loader:.jsx=jsx`. 16/18 pass clean in-sandbox. Unit10_7 and Unit11_1 each had ONE hand-fixed JSX typo (a trailing `\n` leaked into an unclosed `<span>` in a code block — fixed to `</span>{"\n"}`); after the fix esbuild parses all their content with zero errors but the Cowork sandbox mount serves a stale truncated view of these two host-edited files (documented gotcha), so esbuild reports a false "Expected > but found EOF" on the boilerplate closing tags only. Both were verified complete & correct host-side with the Read tool. **Aishu should confirm these two compile on her local `npm run build`** (which is the real gate anyway — the Windows node_modules can't build in the Linux sandbox). No f-strings anywhere; scaffolding respected per each module's position.

**UPDATED lab⇄course coverage — ALL 15 EXPERIMENTS BACKED:** Exp1=M4+M5 ✓; Exp2=M6+M7.1 ✓; Exp3=M7+M8 + **Sets(11.1)+Modules(11.2)** ✓; Exp4=M10.1-2 ✓; Exp5=M10.3+**10.6 abstraction** ✓; Exp6=M10.4+**10.6 polymorphism** ✓; Exp7-9=M9.3-9.4 ✓; Exp10-11=M9.1-9.2 ✓; Exp12=**Unit10_7 user-defined exceptions** ✓; Exp13=**M12.1 Pandas** ✓; Exp14=**M13.1 Matplotlib** ✓; Exp15=M13.1 (Plotly/Seaborn units still to add for full depth).

**REMAINING (optional depth, NOT lab-blocking except Exp15 Seaborn/Plotly):** M13 Unit13_2 Seaborn + Unit13_3 Plotly (finishes Exp15 fully); M11 comprehensions/generators; M12 datetime/JSON/NumPy units; optional Crucibles for M11/M12/M13. Course now spans M1–M13 + three CT interlude sets. Everything registered; Aishu runs npm build/deploy locally.

## SESSION UPDATE 2026-07-05 — Review fixes (M10 numbering + CT1.1 trace)

Aishu confirmed Unit10_7 (user-defined exceptions) and Unit11_1 (sets) are correct as built. Two refinements requested and done this session (lesson-content only, no shell/config/backend change):

- **Module 10 unit numbering corrected.** The inserted units displayed out of order with their breadcrumbs (Polymorphism & Capstone both looked wrong; capstone read "10.5" but appears last). Fixed by editing the visible breadcrumb + completion-title strings ONLY — unitIds/filenames unchanged (they're just config keys; display order = config order). New numbering matches display order: **Unit10_6 (Polymorphism & Abstraction) = UNIT 10.5**, **Unit10_7 (User-Defined Exceptions) = UNIT 10.6**, **Unit10_5 (Capstone: Marks Manager 3.0) = UNIT 10.7**. Config order was already 10_1,10_2,10_3,10_4,10_6,10_7,10_5,10_C — untouched. All three files esbuild-clean in-sandbox.

- **CT Set 1, Unit 1 (`UnitCT1_1.jsx`) accumulator trace reworked** per Aishu's feedback that the "VALUES LEFT" panel was confusing. Changes: (1) removed the `left` field from every trace step and deleted the VALUES LEFT panel; (2) added a fixed **"n · TARGET (never changes)"** panel (purple, value 5) so learners anchor on the goal "sum 1…n"; (3) added the loop-exit reasoning she asked for — the counter now steps to **i = 6** at exit (panel turns red, shows "6 ✕ past n") with a full explanation that range(1, 6) stops before 6, so i moving past n (=5) is *exactly why* the loop ends ("not by magic"); (4) reworded step descriptions from the old "any values left?" framing to an "is i still within 1…n?" loop-check framing. This is the first real-world loop for students, so the exit is now spelled out. No f-strings. Host file is a complete 512 lines; the Linux sandbox mount served a stale 498-line truncated view (documented gotcha) so its esbuild reports a false EOF error — both edits are self-contained and balanced; Aishu's local `npm run build` is the gate.

**Placement note (raised by Aishu):** User-Defined Exceptions sits in M10, not M9, on purpose — defining a custom exception is `class MyError(Exception):`, which needs classes + inheritance (taught in M10.1–10.4). M9 covers try/except/raise with *built-in* exceptions; the custom-exception unit is the earliest point both prerequisites are met, so it's the bridge unit at the end of M10. Left as-is.

## SESSION UPDATE 2026-07-05 (b) — M13 finished (Seaborn + Plotly), M9→M10 forward pointer

Aishu asked to (1) add a forward pointer only (no move) for user-defined exceptions, and (2) build everything the **lab** still needs, explicitly deferring JSON/NumPy/datetime and other optional depth. All done; lesson-content + config only, no shell/backend change.

- **Forward pointer added in `Unit9_2.jsx`** (Catching & Raising). Appended to the `raise`-your-own 🔑 callout: "Coming up: here you're raising Python's built-in errors. Once you learn classes, you'll define your own error types (`class MarksError(Exception)`) — that's Unit 10.6, User-Defined Exceptions." Unit10_7 stays in M10 (needs classes/inheritance); this just links the two. Host file 480 lines, intact (sandbox served a stale truncated view — the recurring mount gotcha).

- **Module 13 (Data Visualization) — NOW COMPLETE. Backs lab Exp 14 & 15 fully.** Two new units, both esbuild-clean (466 lines each, verified with the Linux esbuild binary in /tmp/esbcheck), registered in `course.config.js` under M13 after Unit13_1:
  - **`Unit13_2.jsx` — Statistical Charts with Seaborn** (🌊, MODULE 13 › UNIT 13.2). 5 sections: The Need (plain matplotlib bar vs styled seaborn bar, "sits on top of matplotlib, still plt.show()"), The One-Liner (matplotlib-pull-columns-yourself vs `sns.barplot(data=df, x="Name", y="Marks")` — pass column NAMES, the lower-level contrast), Statistical Charts (live-SVG toggle: **barplot / histplot / boxplot / heatmap**, each with what it reveals statistically — box shows min/Q1/median/Q3/max + outlier, heatmap on df.corr()), The hue Superpower (toggle no-hue vs `hue="Subject"` grouped bars with legend, contrast: manual filtering in matplotlib), Quiz. Completion previews 13.3.
  - **`Unit13_3.jsx` — Interactive Charts with Plotly** (🖱️, MODULE 13 › UNIT 13.3, module-final). 5 sections: The Need (static image vs alive — hover tooltip), The Recipe (`import plotly.express as px`; `fig = px.bar(...)`; `fig.show()` / `fig.write_html()` — returns a fig object, the key contrast vs matplotlib/seaborn drawing immediately), **Charts That Respond** (px.bar/line/scatter/pie with GENUINE live hover tooltips + hover-enlarge/pie-explode via React state — the interactive centerpiece), Interactivity & the Web (hover/zoom, `color=`, `write_html`, plus a Matplotlib→Seaborn→Plotly "one picture" summary), Quiz. Completion announces **"Module 13 Complete"** and points forward to the applied AI/ML, cybersecurity, IoT tracks.

**Lab coverage now: Exp 14 (Matplotlib) = 13.1 ✓; Exp 15 (Seaborn + Plotly) = 13.2 + 13.3 ✓. Every one of the 15 lab experiments is fully backed.**

**Still pending (all OPTIONAL / deferred by Aishu — nothing lab-blocking remains):** M11 comprehensions/generators; M12 datetime / JSON / NumPy units; optional Crucibles for M11–M13; shell — confirm `lesson_complete` reaches the Events tab, build the Looker Studio dashboard. Next substantive horizon (not yet planned): the applied AI/ML, cybersecurity, and IoT tracks. Aishu runs `npm run build`/`deploy` locally (Windows node_modules; Linux sandbox can't run the project build — syntax verified via standalone esbuild only).

## SESSION UPDATE 2026-07-13 — Sets moved from M11 into M7 (collections) + shell dark theme

Two changes this session; both config/lesson/shell only, no backend change.

- **Sets relocated to its container family.** The Sets lesson had been sitting in **M11 "Pythonic Python & the Ecosystem"** (as Unit11_1), separated from the other collections which all live in **M7 "Organizing Data"** (Strings, Lists, List Methods, Tuples & Dictionaries). Aishu flagged this as a mistake and — since the site is still pre-launch and no learner progress needs preserving — asked for a clean renumber. Done:
  - **Files renamed:** `Unit7_5.jsx` (capstone) → `Unit7_6.jsx`; `Unit11_1.jsx` (sets) → `Unit7_5.jsx`; `Unit11_2.jsx` (modules) → `Unit11_1.jsx`. First rename went via `git mv`; the sandbox git index then corrupted (`bad signature`, `index.lock` unlinkable — permission) so the remaining two were done with plain `mv`. **Aishu: on Windows, run `git add -A` (or delete `.git/index.lock` first if it lingers) so git re-picks up the renames — the working tree is correct, only the sandbox's git index is unhappy.**
  - **Inside each renamed file:** the exported component name and the visible `MODULE x › UNIT x.y` breadcrumb were updated to match (7.6 capstone, 7.5 sets, 11.1 modules).
  - **`course.config.js`:** M7 now = 7.1 Strings, 7.2 Lists, 7.3 List Methods, 7.4 Tuples & Dictionaries, **7.5 Sets**, **7.6 Capstone**, 7.C Crucible. M11 now = **11.1 Modules & the Ecosystem** only. (Note: M11 title still reads "Pythonic Python & the Ecosystem" but now holds a single unit — worth reconsidering the module name/whether to fold it elsewhere. Lab Exp 3 coverage unchanged: sets is now 7.5, modules 11.1.)

- **Shell re-themed dark + on-brand.** Landing, Dashboard and Login were cream while every lesson is dark, so the shell now uses a new `DARK` token block in `src/shell/brand.js` (surfaces match the lessons' GitHub-dark greys; amber stays the hero accent). `App.jsx` loading screen and `index.html` theme-color also flipped to dark. Cream tokens kept in brand.js as a fallback.
